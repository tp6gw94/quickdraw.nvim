# Spec: neovim-integration

Module id: `neovim-integration`

## Objective

Provide the user-facing Neovim workflow for creating and reopening Quickdraw PNGs from Markdown. One `:Quickdraw` command inspects the cursor: an inline PNG image link is opened for editing; otherwise the user is prompted for a new drawing name, the editor is opened, and an empty-alt Markdown image link is inserted at the cursor.

This module resolves configuration, document-relative paths, Markdown text, and user notifications. PNG metadata remains owned by `artifact-format`; browser and file persistence remain owned by `editor-session`.

## Tech Stack

- Neovim Lua
- Neovim >= 0.8
- Existing `quickdraw.session` API
- Neovim's built-in user-command, buffer, UI, path, and notification APIs
- Existing Plenary/Busted test harness
- No new runtime dependencies

## User Interface

### Setup

```lua
require("quickdraw").setup({
  path = "./assets",
})
```

`setup()` is optional. The default configuration is:

```lua
{
  path = "./assets",
}
```

Rules:

- `path` is a non-empty string naming the drawing directory and must fit the supported local inline-link form: no whitespace, `()<>?#`, or URI scheme.
- An absolute `path` remains absolute.
- A relative `path` is resolved from the current Markdown file's directory, never from Neovim's working directory.
- Repeated `setup()` calls replace the previous configuration from defaults rather than retaining omitted old values.
- Unknown keys and invalid values fail immediately without changing the active configuration.
- `~`, environment variables, and glob characters are not expanded by the plugin.

`require("quickdraw").setup()` is the only public Lua entrypoint. `quickdraw.session.start()` remains an internal integration boundary and is not part of user configuration or usage documentation.

### Command

```vim
:Quickdraw
```

The command takes no arguments and follows this decision order:

1. Require a Markdown buffer.
2. Inspect the current line and byte-indexed cursor column.
3. If the cursor is within a supported inline Markdown image whose destination is a local `.png` path, resolve that destination from the document directory when relative and open it through `quickdraw.session.start()`.
4. Otherwise call `vim.ui.input()` for a drawing name.
5. On a valid name, create the configured directory if necessary, start the editor session for the resolved target, then insert the Markdown image at the original cursor position.
6. Cancellation or an empty prompt result is a no-op.

Starting a command while another editor session is active retains the existing `editor-session` replacement semantics.

### Supported cursor link

Cursor editing deliberately supports the minimum inline form generated by this plugin:

```markdown
![](./assets/diagram.png)
```

Rules:

- The image must be entirely on the current line.
- The cursor may be anywhere from `!` through the closing `)`.
- The destination must be a non-empty local filesystem path ending in `.png`, case-insensitively.
- Relative destinations resolve from the current Markdown file's directory; absolute destinations remain absolute.
- The linked PNG does not have to live under the configured creation directory.
- URI schemes, query strings, fragments, reference-style images, titles, multiline images, angle-bracket destinations, whitespace in destinations, and nested parentheses are not parsed.
- A PNG link is treated as an edit candidate. Missing files create a new drawing at that linked path, existing Quickdraw PNGs open, and ordinary or invalid PNGs surface the existing session validation error rather than switching to the name prompt.

### Drawing names and insertion

The prompt accepts a basename such as `diagram` or `diagram.png`.

Rules:

- Trim surrounding whitespace.
- Reject an empty name, `.` or `..`, path separators, NUL/control bytes, whitespace, and Markdown delimiters `[]()<>`.
- Treat a final `.png` suffix case-insensitively and normalize it to lowercase; otherwise append `.png`.
- Always leave the generated Markdown alt text empty; the prompted name only determines the PNG filename.
- Other dots are part of the basename, so `architecture.v2` becomes `architecture.v2.png`.
- Existing Quickdraw PNGs open for editing; existing ordinary or invalid PNGs are not overwritten.
- Create missing configured directories recursively before starting the session.
- Insert only after `quickdraw.session.start()` succeeds. A browser-launch warning still counts as a started session.
- Insert exactly at the original cursor byte position and leave the buffer modified for the user to save.

For `setup({ path = "./assets" })` and the name `diagram`, insert:

```markdown
![](./assets/diagram.png)
```

For an absolute configured path, insert an absolute destination. Markdown destinations use `/` separators on every platform; filesystem operations use the platform path accepted by Neovim.

## Error Behavior

- User-facing failures use `vim.notify()` with the `quickdraw.nvim` title and an appropriate warning or error level.
- Notifications use stable, sanitized plugin/session messages and never include the loopback bearer token.
- Invalid setup raises a Lua configuration error synchronously.
- Command failures do not modify the Markdown buffer.
- Relative path resolution from an unnamed or unsaved buffer fails with a clear notification.
- Directory creation failure, invalid name, unsupported buffer/link, or session failure leaves existing files unchanged.
- When the default browser cannot be opened, keep the session active and notify the user with the returned local URL so it can be opened manually.

## Commands

```text
All tests: make test
Focused:   nvim --headless --noplugin -u tests/minimal_init.lua -c "PlenaryBustedFile tests/quickdraw/integration_spec.lua"
Format:    stylua lua tests
Lint:      stylua --check lua tests
Build:     none
```

## Project Structure

```text
lua/
├── quickdraw.lua              Public setup and command workflow
└── quickdraw/
    ├── png.lua                Existing artifact module
    ├── session.lua            Existing editor-session module
    └── session_state.lua      Existing process-wide session state

plugin/
└── quickdraw.lua              Registers :Quickdraw once

tests/
└── quickdraw/
    └── integration_spec.lua   Config, path, Markdown, command, and error tests

README.md                      Installation and usage
doc/quickdraw.txt              Generated/help documentation
SPEC-neovim-integration.md     This contract
```

The remaining `plugin_name` template modules, tests, command, and documentation are removed when their Quickdraw replacements exist.

## Code Style

```lua
local M = {}

local defaults = {
  path = "./assets",
}

function M.setup(options)
  local next_config = validate_config(options or {})
  config = vim.tbl_extend("force", defaults, next_config)
end

return M
```

Conventions:

- Modules and functions use `snake_case`.
- Constants use `UPPER_SNAKE_CASE`.
- Validate setup, prompt, Markdown, and filesystem inputs at their boundaries.
- Expected runtime failures use existing structured session errors internally and concise notifications at the command boundary.
- Helpers remain private; do not add abstractions with only speculative consumers.

## Testing Strategy

Use the existing Plenary/Busted environment and TDD for every behavior.

Focused tests cover:

1. Default, relative, absolute, repeated, and invalid setup behavior.
2. Relative paths resolve from the Markdown document directory despite `:cd`, `:lcd`, or `:tcd` changes.
3. POSIX, Windows drive, and Windows UNC path classification and Markdown separator normalization.
4. Cursor positions across a generated inline image resolve the intended PNG.
5. Unsupported Markdown forms and non-image text fall back to the name prompt.
6. Prompt cancellation and empty input are no-ops.
7. Valid names normalize to one `.png`; traversal, separators, controls, whitespace, and Markdown delimiters are rejected.
8. Missing directories are created; directory errors do not insert text or start a session.
9. Successful creation starts the fixed absolute target and inserts the expected link at the original byte position, including Unicode text before the cursor.
10. Existing Quickdraw PNGs open; ordinary PNGs and target conflicts retain existing session errors and do not alter the buffer.
11. Browser launch failure keeps the URL available without exposing the token in unrelated errors.
12. `:Quickdraw` is registered once and accepts no arguments.
13. Existing PNG and editor-session tests remain green.

A manual Neovim check creates, saves, closes, and reopens a drawing from a Markdown link. The existing Linux/macOS/Windows and Neovim 0.8/stable/nightly CI matrix is the compatibility gate.

## Boundaries

### Always

- Resolve relative creation and edit paths from the current Markdown document directory.
- Pass only an absolute fixed PNG path to `quickdraw.session.start()`.
- Validate names and local Markdown destinations before filesystem access.
- Preserve the existing loopback token, singleton, preflight, conflict, and atomic-save guarantees.
- Keep all runtime behavior offline and dependency-free.
- Leave Markdown buffer saving under user control.

### Ask First

- Add another command, setup option, keymap, picker, or automatic save.
- Expand support to reference-style, multiline, titled, remote, or full CommonMark image parsing.
- Change where relative paths are based.
- Insert links before a session starts successfully.
- Add a runtime dependency or modify the PNG/session contracts.

### Never

- Resolve relative paths from Neovim's current working directory.
- Accept a prompted path or allow the browser to choose a path.
- Overwrite an existing ordinary or invalid PNG.
- Execute shell-interpolated paths or URLs.
- Put paths or document locations into PNG metadata.
- Expose the session bearer token in generic error messages or logs.

## Success Criteria

- `require("quickdraw").setup({ path = "./assets" })` configures document-relative storage, with the same value as the default.
- One `:Quickdraw` command creates from ordinary cursor positions and edits from supported PNG image links.
- New drawings insert the expected Markdown image at the original cursor position only after the session starts.
- Existing Quickdraw PNGs reopen as editable drawings; ordinary PNGs are never overwritten.
- Relative behavior is independent of Neovim's working directory.
- Browser-launch failure leaves a usable URL.
- Template plugin behavior and branding are removed.
- README/help explain installation, configuration, creation, editing, and errors.
- Focused, full, formatting, diff, manual, and declared CI compatibility checks pass.

## Source Evidence

- Neovim 0.8 user command API: https://github.com/neovim/neovim/blob/v0.8.0/runtime/doc/api.txt#L1728
- Neovim 0.8 partial-line buffer edits: https://github.com/neovim/neovim/blob/v0.8.0/runtime/doc/api.txt#L2391
- Neovim 0.8 cursor indexing: https://github.com/neovim/neovim/blob/v0.8.0/runtime/doc/api.txt#L2784
- Neovim 0.8 `vim.ui.input()`: https://github.com/neovim/neovim/blob/v0.8.0/runtime/doc/lua.txt#L2011
- Neovim 0.8 `vim.notify()`: https://github.com/neovim/neovim/blob/v0.8.0/runtime/doc/lua.txt#L1445
- Neovim 0.8 `fnamemodify()`: https://github.com/neovim/neovim/blob/v0.8.0/runtime/doc/builtin.txt#L2398
- Neovim 0.8 recursive `mkdir()`: https://github.com/neovim/neovim/blob/v0.8.0/runtime/doc/builtin.txt#L5421

## Open Questions

None.
